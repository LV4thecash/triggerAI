# TriggerAI

This repository contains the NOVA TRIGGER Chrome extension located in
`nova-trigger/`.

To run the extension locally:

```bash
cd nova-trigger
npm install
npm run build
```

Then load the `nova-trigger/dist` directory as an unpacked extension in Chrome.
